const intialstate = {
    result: [],
    status: ''
}

function storeUser(state = intialstate, action) {
    switch (action.type) {
        
            default:
                    return state;

    }
}

export default storeUser;